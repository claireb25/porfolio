{{ form_start(form) }}
    {{ form_widget(form) }}
    <div class="flex-button">
        <button class="margin-new new">{{ button_label|default('Enregistrer') }}</button>
    </div>
{{ form_end(form) }}