{% extends 'base.html.twig' %}

{% block title %}Dates{% endblock %}

{% block body %}
    <h1>Dates</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ date.id }}</td>
            </tr>
            <tr>
                <th>Year</th>
                <td>{{ date.year }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('dates_index') }}">back to list</a>

    <a href="{{ path('dates_edit', {'id': date.id}) }}">edit</a>

    {{ include('dates/_delete_form.html.twig') }}
{% endblock %}